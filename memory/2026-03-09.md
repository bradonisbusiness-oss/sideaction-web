# 2026-03-09 - Session Notes

## Identity & Setup
- I am Luka, Bradon's AI business partner / future CEO
- Bradon's username: bradontok, Telegram ID: 478343251
- Goal: $1,000/day revenue. No upfront capital. 6 hrs/day from phone.
- Values: revenue first, speed > perfection, distribution > product, leverage via systems
- Language level: 3rd-5th grade. No fluff.

## SideAction App
- React Native (Expo) mobile app for two-person bet tracking with social feed
- Original Replit build: local AsyncStorage only — two-person sync broken
- Extracted code to: `C:\Users\tarik\.openclaw\workspace\sideaction`
- Supabase project: https://cdrmswrqaydykvcvkphi.supabase.co
- Supabase anon key: sb_publishable_9TF6REmRZ3pxhW2BVoI0CQ_r2TShkui

### Backend Migration (COMPLETED by subagent)
Files created/modified:
- `supabase/schema.sql` — full DB schema with RLS
- `lib/supabase.ts` — Supabase client
- `context/AuthContext.tsx` — auth state
- `app/auth/login.tsx` + `app/auth/signup.tsx`
- `app/_layout.tsx` — auth redirect
- `context/BetsContext.tsx` — full rewrite with Supabase + real-time sync
- `package.json` — added @supabase/supabase-js

### Supabase Setup (ALL COMPLETED ~3:00 PM CDT)
1. ✅ Ran `supabase/schema.sql` in Supabase SQL Editor
2. ✅ Enabled Realtime for: bets, notifications, bet_reactions, comments
3. ✅ Disabled email confirmation in Supabase Auth
4. ✅ Ran `npm install --legacy-peer-deps`
5. ✅ App runs via `npx expo start --tunnel` (phone on hotspot, no WiFi)
6. ✅ Bradon signed up successfully, profile screen loads
7. ✅ Fixed ProfileScreen crash: `displayName[0].toUpperCase()` — added null fallback

## Discord Bot Setup (IN PROGRESS)
- Bot name: Luka
- App/Bot client ID: 1480621737296269583
- Bot token: set in OpenClaw config
- Bradon's Discord User ID: 826321289455206411
- Bradon's Discord Server ID: 1480625572127445147
- Bot IS logged in (confirmed in logs)
- Issue: bot hasn't joined the server yet — Bradon needs to re-run the OAuth invite URL targeting the correct server
- Invite URL: https://discord.com/oauth2/authorize?client_id=1480621737296269583&permissions=117760&integration_type=0&scope=bot+applications.commands

### Next steps for Discord
1. Bradon uses invite URL and selects correct server
2. DM the bot → get pairing code → approve pairing
3. Configure guild allowlist with Server ID + User ID

## Session 2 Updates (afternoon, Discord channel #sideaction)
- Moved from Telegram to Discord for all future conversations
- Discord channel: #sideaction, channel ID: 1480634164012388444

### Tagline finalized
- Hook: "Bet. Track. Collect."
- Subtitle: "No more 'I don't remember.' Every deal is on record."
- Both on login + signup screens

### Logo
- Bull skull with spade on forehead, circular frame
- Dark background removed version saved to: `assets/images/logo.png`
- Used on login + signup screens (120x120)

### Connections Feature (COMPLETED)
- Added `connections` table to Supabase (`supabase/connections_only.sql`)
- Tier system: 'crew' (default) and 'day_ones'
- Files created:
  - `context/ConnectionsContext.tsx` — fetch, realtime, add/remove/promote/demote
  - `app/(tabs)/connections.tsx` — Connections screen with Day Ones + Crew sections
  - `app/invite.tsx` — Invite friends screen (share link, copy link, QR)
- Profile screen shows "X Crew · Y Day Ones" counts
- Invite Friends button added to profile above sign out
- Long-press on connection → promote/demote/remove options

### Bradon preferences confirmed
- No fluff, no filler phrases ("quick sanity check", "great question", etc.)
- Blunt, honest, direct only
- He is now on laptop (was phone-only before)

## Notes
- Wants to hand off as many tasks as possible to me
- No ego — only goal is revenue
- Next: test invite flow with a second user, then continue building

## Session 3 Updates (evening, ~10:30 PM–11:20 PM CDT)

### Navigation / NativeTabs Bug (RESOLVED)
- App was using `NativeTabs` from `expo-router/unstable-native-tabs` (Liquid Glass layout on iOS)
- This blocked all `router.push()` navigation — taps acknowledged visually but no navigation occurred
- Fix: forced `ClassicTabLayout` only in `app/(tabs)/_layout.tsx` (removed NativeTabs condition)

### Connections Screen Refactor
- Moved `app/(tabs)/connections.tsx` → `app/connections.tsx` (stack screen, not tab)
- Registered in `app/_layout.tsx` as `<Stack.Screen name="connections" presentation="card" />`
- Added `useLocalSearchParams` filter support (`filter: "crew" | "day_ones"`)
- Each connection row now has a **Bet** button → navigates to create-bet with opponentId/opponentName

### Add Friend Modal (on Profile screen)
- Navigation to connections screen was unreliable, so search was moved inline
- Bottom sheet modal opens directly on Profile when tapping person+ button
- Modal has: search input, live results from Supabase profiles table, Add/Added button
- State managed in ProfileScreen: `searchModal`, `searchQuery`, `searchResults`, `isSearching`
- `addConnection()` called directly from modal
- Bypassed all navigation problems entirely

### Profile Header Buttons
- Added person-add-outline button (44x44, blue) to profile header top-right
- QR button still exists next to it
- Switched from `Pressable` to `TouchableOpacity` for header buttons (NativeTabs was blocking Pressable)
- Crew and Day Ones text made bigger (fontSize 13, SemiBold), individually tappable

### File State
- `app/(tabs)/profile.tsx` — search modal added, TouchableOpacity buttons, larger crew/day ones text
- `app/(tabs)/_layout.tsx` — NativeTabs disabled, ClassicTabLayout only
- `app/connections.tsx` — new stack screen (copy of tabs version + filter param + Bet button)
- `app/_layout.tsx` — connections registered as Stack.Screen
- `app/(tabs)/connections.tsx` — still exists (unused for now, kept as backup)
