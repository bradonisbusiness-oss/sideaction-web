# 2026-03-10 - Session Notes

## SideAction App - Dev Session

### SQL Migrations Run in Supabase
- `supabase/add_connection_status.sql` ✅ — added `status` column to connections table
- `supabase/loans.sql` ✅ — created loans table with RLS policies
- `supabase/challenges.sql` ✅ — created challenges + challenge_participants tables with RLS
- Fixed incomplete DELETE policy on loans table (dropped and recreated with correct USING clause)

### Code Fixes Applied
- `app/(tabs)/profile.tsx`: Added `useEffect` to React import (was missing, caused render crash)
- `app/(tabs)/profile.tsx`: Reduced `scoreNumber` fontSize from 24 → 18 (100% was wrapping to 2 lines in circle)
- `app/(tabs)/profile.tsx`: Changed "Untested" rep score color from purple `#A78BFA` → blue `#007AFF`
- `app/(tabs)/profile.tsx`: Added `sentIds` local state to track sent friend requests so Add button shows "Sent ✓" immediately
- `app/connections.tsx`: Added `addSearchRef` to the add-person TextInput
- `app/connections.tsx`: Added `+` icon buttons beside Day Ones and Crew section headers (focuses add search bar on tap)
- `app/connections.tsx`: Added `addIconBtn` style

### Expo Setup
- Running via `npx expo start` from `C:\Users\tarik\.openclaw\workspace\sideaction`
- Phone hotspot + tunnel mode broken (ngrok error: `Cannot read properties of undefined (reading 'body')`)
- Web mode working: `npx expo start --web` → http://localhost:8081
- Tunnel: `@expo/ngrok` package has a bug, not fixed yet

### In Progress at End of Session
- "Add friend" button still not working — trying to debug via browser DevTools console
- Asked Bradon to open http://localhost:8081, F12, Console tab, click Add, screenshot errors
- Suspect RLS policy or auth.uid() mismatch on connections insert

### App Status
- Bradon is running on PC (web) + phone (Expo Go via hotspot, spotty)
- Project path: `C:\Users\tarik\.openclaw\workspace\sideaction`
- Supabase project: https://cdrmswrqaydykvcvkphi.supabase.co
