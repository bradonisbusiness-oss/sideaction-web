# Deploy Steps

Run these in PowerShell:

cd C:\Users\tarik\.openclaw\workspace\sideaction-web
git init
git add .
git commit -m "initial landing page"
git remote add origin https://github.com/bradonisbusiness-oss/sideaction-web.git
git push -u origin master
